<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>AirBoost Shoes</title>
  <style>
    body { font-family: Arial; background: #f4f4f4; }
    .grid { display: flex; flex-wrap: wrap; gap: 20px; }
    .item { background: #fff; padding: 10px; width: 200px; border-radius: 10px; box-shadow: 0 0 5px rgba(0,0,0,0.1); }
    img { width: 100%; border-radius: 10px; }
    .btn { display: block; margin-top: 10px; background: #222; color: #fff; padding: 8px; text-align: center; border-radius: 5px; text-decoration: none; }
  </style>
</head>
<body>
  <h1>AirBoost Shoes</h1>
  <div class="grid">
    <% products.forEach(p => { %>
      <div class="item">
        <img src="<%= p.image %>" alt="<%= p.name %>">
        <h4><%= p.name %></h4>
        <p>€<%= p.price %></p>
        <a class="btn" href="https://t.me/stanman101?text=I would like to buy your sneakers<%= encodeURIComponent(p.name) %> за <%= p.price %>€">Order via Telegram</a>
      </div>
    <% }) %>
  </div>
</body>
</html>
